use Libraries.Game.Game
use Libraries.Game.Graphics.Color
use Libraries.Game.Graphics.Drawable

class Main is Game 
    Drawable bar
    Clickable startMenu

    action Main
        EnableResizing(true)
        StartGame()
    end

    action CreateGame
        startMenu:Load("resources/startMenu.png")
        //startMenu:SetPosition(1280, 720)
        Add(startMenu)
    end

    action Update(number seconds)
    end

end